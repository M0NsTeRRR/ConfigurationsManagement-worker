---
- name: GET list of zones
  uri:
    url: "{{ powerdns_base_url }}/servers/localhost/zones"
    method: GET
    return_content: yes
    body_format: json
    status_code: 200
    headers:
      X-Api-Key: "{{ powerdns.api_key }}"
  register: zones